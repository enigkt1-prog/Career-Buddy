import { test, expect } from "@playwright/test";

// Placeholder spec — keeps Playwright runner green before Phase 4
// adds the real lazy-chunks assertion. Iter-3 plan v5 reference: Phase 0.
test("playwright rig is wired", async () => {
  expect(1).toBe(1);
});
